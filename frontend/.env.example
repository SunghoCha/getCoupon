# 프로젝트를 처음 받으면 이 파일을 `.env`로 복사한 뒤 값을 채워 사용한다.
# axios baseURL과 OAuth 진입 URL이 사용한다.
# 개발: http://localhost:8080
# 운영: 운영 백엔드 도메인 (예: https://api.letterpick.com), .env.production에 둔다
VITE_API_BASE_URL=http://localhost:8080
